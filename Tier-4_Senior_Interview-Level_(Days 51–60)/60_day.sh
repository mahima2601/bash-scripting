#!/bin/bash


#Day 60. Capstone: Write a mini deployment orchestrator that: validates prerequisites, takes an environment flag, pulls/templates a config, runs a deploy command with retries and logging, health-checks the result, and rolls back on failure — all with strict mode, traps, and clean logging. Concept: everything combined. This is essentially a real script you'd write at work.
