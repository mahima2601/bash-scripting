#!/bin/bash


#Day 59. Write a script that diffs two environments by comparing the sorted output of two commands (e.g. installed package lists) and prints what's only in each. Concept: comm, diff, process substitution <( ).
