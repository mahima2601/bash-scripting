#!/bin/bash


#Day 55. Demonstrate and explain ${PIPESTATUS[@]}: write a pipeline where the first command fails and show how to detect it even though the pipeline "succeeds". Concept: pipeline exit codes — a classic gotcha senior interviewers probe.
