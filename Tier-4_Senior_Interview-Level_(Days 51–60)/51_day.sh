#!/bin/bash


#Day 51. Implement exponential backoff with jitter for a retried command (delays 1s, 2s, 4s, 8s… capped at 30s, plus a small random offset). Concept: backoff algorithm, $RANDOM, arithmetic.
