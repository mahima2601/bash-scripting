#!/bin/bash


#Day 50. Write a script that bumps a semantic version (MAJOR.MINOR.PATCH) — accepting major|minor|patch as an argument — reading from and writing to a VERSION file. Concept: parsing, arithmetic, file I/O. Hint: IFS=. read -r major minor patch < VERSION.
