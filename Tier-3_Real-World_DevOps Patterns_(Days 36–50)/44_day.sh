#!/bin/bash


#Day 44. Write a log-rotation script: when a log exceeds a size threshold, rename it with a timestamp, gzip it, and truncate the original. Concept: size checks, gzip, : > file truncation.
