#!/bin/bash


#Day 49. Write a deployment helper that checks a list of prerequisites (commands installed, env vars set, cluster reachable) and fails fast with a clear list of what's missing. Concept: validation, accumulating errors, command -v.
